procs:
  # ngrok:
  #   shell: "ngrok http http://localhost:2727"
  supabase_status:
    shell: "supabase status"
  supabase_functions:
    shell: "supabase functions serve"
  # supabase_functions:
  #   shell: "deno --allow-net --allow-read --allow-write scripts/router-url-env.ts && supabase functions serve"
  astro:
    shell: "pnpm start"
    cwd: "apps/astro-app"
  expo:
    shell: "pnpm start"
    cwd: "apps/expo-app"
  expo-tsc:
    shell: "pnpm tsc"
    cwd: "apps/expo-app"
  router-handler:
    shell: "deno task dev"
    cwd: "apps/ridi-router/router-handler"
  map-data-handler:
    shell: "deno task dev"
    cwd: "apps/ridi-router/map-data-handler"
